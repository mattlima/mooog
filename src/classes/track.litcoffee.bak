## Track

A convenience function that creates a chain with the specified node(s)
followed by a panner/gain stage.

    class Track


`id`: String identifier, used to refer to this track after creation.
`_instance`: The parent `Mooog` instance
`params`: An optional object of initialization parameters for the track

      constructor: (@id, @_instance, params) ->
        @_nodes = []
        @_panner = @_instance.context.createPanner()
        @_gain = @_instance.context.createGain()
        @_panner.connect @_gain
        @_gain.connect @_instance._destination



### Track.add
`type`: String indicating node type. Equivalent to the part of the
node name before "Node", ex. "Oscillator"
`id`: Optional string identifier for this node
`ord`: Optional point in the chain at which to insert. Defaults
to just before gain/pan stage
`param`: object of initialization parameters

      add: (type, id, ord, params) ->
        switch type
          when "Oscillator"
            @_nodes.splice ord, 0, new Oscillator(this, params)
            @_nodes[0].connect @_panner

          else throw new Error "Unknown AudioNode type: #{type}"


        @


